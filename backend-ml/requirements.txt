# Core ML
contourpy>=1.3.1
cycler==0.12.1
fonttools>=4.53.0
joblib>=1.4.2
kiwisolver>=1.4.7
matplotlib>=3.9.2
numpy>=2.1.0
pandas>=2.2.2
scikit-learn>=1.5.0
seaborn>=0.13.2

# Vector DB (RAG memory)
chromadb>=0.5.5

# Embeddings (sentence-transformers for local vector search)
sentence-transformers

# Image processing (for Gemini multimodal visualization insights)
Pillow>=10.0.0

# API Framework
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0
python-multipart
python-dotenv

# Auth
pyjwt

# Cloud LLM — Primary: Groq (open-source Gemma via API, 15k TPM free tier)
groq>=0.9.0

# Cloud LLM — Vision only: Gemini (kept ONLY for visualization chart image analysis)
# Groq does not support multimodal/image input yet, so Gemini handles that one route.
google-generativeai

# HTTP (for HuggingFace API fallback)
requests

# NOTE: ollama is NOT listed here because it requires a local Ollama
# server to be running (not available on cloud platforms).
# For local development only: pip install ollama
# Local LLM Support
ollama
